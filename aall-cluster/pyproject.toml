[project]
name = "aall-cluster"
version = "0.1.0"
description = "A colorful terminal monitor for the AALL HTCondor cluster."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
aall-cluster = "aall_cluster.__main__:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
